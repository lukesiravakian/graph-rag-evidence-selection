{
  "config": {
    "model": "all-mpnet-base-v2",
    "k": 5,
    "candidate_size": 15,
    "num_questions": 18,
    "methods": [
      "top_k",
      "mmr",
      "facility_location"
    ]
  },
  "comparison": {
    "top_k": {
      "precision@k": 0.162962962962963,
      "recall@k": 0.7777777777777778
    },
    "mmr": {
      "precision@k": 0.14444444444444446,
      "recall@k": 0.7222222222222222
    },
    "facility_location": {
      "precision@k": 0.08888888888888888,
      "recall@k": 0.4444444444444444
    }
  },
  "predictions": {
    "top_k": [
      "Requests uses the `extract_cookies_to_jar` function to pull cookies from an HTTP response by wrapping the request and original response headers into `MockRequest` and `MockResponse` objects. It then delegates the extraction and storage to the `CookieJar` instance by calling its `extract_cookies` method.",
      "Requests uses the `remove_cookie_by_name` function to iterate through a `CookieJar` and collect all cookies matching the given name across domains and paths. It then removes each identified cookie by calling `CookieJar.clear()` with its specific domain, path, and name.",
      "The `RequestsCookieJar` class provides a dict-like interface while remaining compatible with Python's `http.cookiejar.CookieJar`. It inherits from both `CookieJar` and `MutableMapping` to support dictionary-style operations alongside standard cookie jar functionality.",
      "Requests uses the `create_cookie` function to generate a `Cookie` object from a given name and value. This is called in methods like `RequestsCookieJar.set()` or helper functions like `cookiejar_from_dict()`.",
      "`requests` constructs the value for the HTTP Basic Auth header by passing the username and password to the `_basic_auth_str` helper function. This function is called directly within class methods like `HTTPBasicAuth.__call__` and `HTTPProxyAuth.__call__` (or the `proxy_headers` function) to set the `Authorization` or `Proxy-Authorization` headers.",
      "The `HTTPDigestAuth` class implements HTTP Digest Authentication in `requests`. It manages nonces through methods like `build_digest_header` and handles unauthorized 401 responses using `handle_401`.",
      "`requests` resolves configuration conflicts by prioritizing per-request settings over session-level settings. This merging logic is handled by helper functions like `merge_setting` within `Session.prepare_request` and `Session.merge_environment_settings`.",
      "Requests uses `merge_hooks` instead of standard merging because when `request_hooks` is passed as `{'response': []}`, standard merging breaks `Session` hooks entirely. The `merge_hooks` function explicitly handles empty hook lists to preserve the session's hooks before falling back to `merge_setting`.",
      "In `SessionRedirectMixin.rebuild_auth`, the `Authorization` header is stripped if it is present in the request and `should_strip_auth` returns `True`. When redirecting to a request with a different host, `should_strip_auth` compares the URL hostnames (`old_parsed.hostname != new_parsed.hostname`) and returns `True` to prevent credential leakage.",
      "In `SessionRedirectMixin.resolve_redirects`, `requests` loops through redirect targets, extracting and merging cookies onto each copied request using `extract_cookies_to_jar` and `merge_cookies`. At each hop, it re-evaluates connection configurations by invoking `rebuild_proxies` and `rebuild_auth` (which utilizes `should_strip_auth` to prevent leaking credentials across host boundaries) before transmitting the request via `self.send`.",
      "When handling redirects in `resolve_redirects`, `Session.rebuild_proxies` calls `resolve_proxies` to re-evaluate proxy configuration against the new target URL and bypass proxies if rules like `NO_PROXY` apply. Additionally, `rebuild_proxies` resets and regenerates the `Proxy-Authorization` header for non-HTTPS requests based on the new proxy settings.",
      "In `get_redirect_target`, `requests` checks if a response is a redirect using `is_redirect` and retrieves the `\"location\"` string from `Response.headers`. It then re-encodes the header value from `latin1` to `utf8` using `to_native_string` to correctly process any non-ASCII or UTF-8 target URLs.",
      "In `prepare_url`, the URL is parsed with `parse_url` to validate that a scheme and host exist, raising `MissingSchema` or `InvalidURL` for invalid formats. For non-ASCII hostnames, `prepare_url` calls `_get_idna_encoded_host` to IDNA-encode the host before reconstructing and normalizing the final URL using `urlunparse` and `requote_uri`.",
      "In `prepare_body`, requests checks the provided arguments in sequence to determine the request body format:\n- If `data` is omitted and `json` is specified, it encodes the JSON payload and sets `Content-Type` to `application/json`.\n- If `data` is a non-standard iterable (like a file-like object or generator), it sends it as a streamed body (setting `Content-Length` or `Transfer-Encoding: chunked`), whereas if `files` are present it calls `_encode_files` for multipart uploads, or otherwise url-encodes `data` as form data (`application/x-www-form-urlencoded`).",
      "A `Response` object determines if it is a redirect via its `is_redirect` property (or `is_redirect` method), which checks if a `\"location\"` header is present in `self.headers` and if `self.status_code` is listed in `REDIRECT_STATI`. Additionally, the `is_permanent_redirect` property checks if the status code specifically corresponds to a permanent redirect (`codes.moved_permanently` or `codes.permanent_redirect`).",
      "When no encoding is explicitly set, the `text` property falls back to `self.apparent_encoding` to guess the encoding using `charset_normalizer` or `chardet`. Additionally, `get_encoding_from_headers` defaults to `\"ISO-8859-1\"` for text content types or `\"utf-8\"` for `application/json` when the `charset` parameter is missing from the headers.",
      "`resolve_proxies` checks if the URL should bypass proxies using `should_bypass_proxies`, which evaluates `no_proxy` settings and system bypass rules. If proxies are not bypassed, `get_environ_proxies` retrieves system proxy configurations using `getproxies()`, which are then matched against the URL scheme and hostname using `select_proxy`.",
      "Under the hood, the top-level `get` function in `api.py` forwards its parameters to `api.py::request()` with the HTTP method set to `\"get\"`. `api.py::request()` then instantiates a `sessions.Session()` context manager and calls its `session.request()` method to execute the request."
    ],
    "mmr": [
      "Requests extracts cookies from a response using the `extract_cookies_to_jar` function. It wraps the request and original response headers into `MockRequest` and `MockResponse` objects before calling `CookieJar.extract_cookies` to populate the jar.",
      "To remove a cookie by name across all domains and paths, requests uses the `remove_cookie_by_name` function (which is also wrapped by `RequestsCookieJar.__delitem__`). It iterates through the `CookieJar` to identify matching cookies, collects their domain, path, and name into a list, and then deletes them by invoking `cookiejar.clear()` for each entry.",
      "The `RequestsCookieJar` class provides requests' cookie jar with a dict-like interface while remaining compatible with Python's `http.cookiejar.CookieJar`. It inherits from both `CookieJar` and `MutableMapping` to allow dictionary-style cookie operations.",
      "When provided with a name and string value, `RequestsCookieJar.set` constructs a cookie using the `create_cookie` helper function. If passed a `Morsel` object instead, it converts it using `morsel_to_cookie`.",
      "Requests constructs the HTTP Basic Auth header value by passing the username and password to the `_basic_auth_str` function. This value is then attached to the `Authorization` header via the `HTTPBasicAuth` class (or to `Proxy-Authorization` in `proxy_headers`).",
      "`HTTPDigestAuth` implements HTTP Digest Authentication by building authentication headers with nonce tracking in `build_digest_header`. It manages unauthorized server challenges by intercepting 401 responses and retrying requests using its `handle_401` method.",
      "Requests uses the `merge_setting` function in the `Session` class to resolve conflicts by prioritizing per-request settings over session-level settings. For non-dictionary settings, the explicit per-request value directly overrides the session value, while for dictionary settings, `merge_setting` merges the two dictionaries so that per-request key-value pairs overwrite session values (or delete keys set to `None`).",
      "Requests uses the `merge_hooks` function because standard merging breaks `Session` hooks when `request_hooks` contains empty lists like `{'response': []}`. The special logic ensures that empty request hook lists do not overwrite or break existing session hooks.",
      "In the `should_strip_auth` method, `requests` strips the `Authorization` header whenever the hostname of the new URL differs from the original URL (`old_parsed.hostname != new_parsed.hostname`). Under this condition, `should_strip_auth` immediately returns `True` to remove the header for cross-host redirects.",
      "`SessionRedirectMixin.resolve_redirects` repeatedly processes redirect URLs in a loop, copying the request and updating its configuration at each hop. It adjusts the HTTP method via `rebuild_method`, updates proxy and authentication headers via `rebuild_proxies` and `rebuild_auth` (using `should_strip_auth` to remove credentials when changing hosts), and handles state persistence using `extract_cookies_to_jar` and `merge_cookies`.",
      "During redirect handling in `resolve_redirects`, `requests` updates the target URL on the prepared request and calls `rebuild_proxies` to recompute the proxy settings for the new destination. The resulting proxy configuration, which respects bypass rules checked by `should_bypass_proxies`, is then passed into `Session.send` for the redirected request.",
      "The target URL is extracted using the `get_redirect_target` method, which checks if `resp.is_redirect` is true and retrieves the `\"location\"` header from the `Response` object. To handle UTF-8 characters correctly, it re-encodes the `latin1` header string using `to_native_string`.",
      "In `prepare_url`, `requests` parses and validates the URL's scheme and host using `parse_url`, ensuring required components are present before normalizing the URL with `urlunparse` and `requote_uri`. If the host contains non-ASCII characters, `prepare_url` invokes `_get_idna_encoded_host` to UTS46-encode the domain name using the `idna` library.",
      "In `requests`, the request format is determined by the keyword arguments passed to `request()`, such as `json`, `data`, or `files`. When `files` is provided, `_encode_files()` constructs a `multipart/form-data` body using `encode_multipart_formdata`, while options like `data` or `json` specify standard form-encoded data or JSON payloads respectively.",
      "A `Response` object determines if it represents an HTTP redirect through its `is_redirect` property. It checks whether a `\"location\"` key exists in `self.headers` and if `self.status_code` is contained within `REDIRECT_STATI`.",
      "Requests determines a response's text encoding through `apparent_encoding`, which uses character detection libraries like `chardet` on `self.content` (falling back to `\"utf-8\"` if unavailable). For raw JSON byte data, `guess_json_utf` inspects byte order marks (BOMs) and null byte counts to identify the specific UTF encoding.",
      "In `resolve_proxies`, if `trust_env` is `True` and `should_bypass_proxies` confirms the URL should not bypass proxy settings, `requests` fetches environment configuration via `get_environ_proxies`. It then selects the proxy matching the URL's scheme (or the fallback `\"all\"` proxy) and assigns it to `new_proxies`.",
      "The top-level `get()` function in `api.py` acts as a wrapper that sends an HTTP GET request by delegating the call to the `request()` function with the method set to `\"get\"`. It passes along the target `url`, query parameters (`params`), and any additional keyword arguments to execute the request."
    ],
    "facility_location": [
      "Requests wraps a `PreparedRequest` inside a `MockRequest` object to mimic the `urllib2.Request` interface expected by Python's standard `http.cookiejar.CookieJar`. This allows `CookieJar` to evaluate domain policies and store response cookies in a `RequestsCookieJar` or `CookieJar` instance.",
      "Requests uses the `remove_cookie_by_name` function (also wrapped by `RequestsCookieJar.__delitem__`), which iterates through the cookie jar to collect matching domain, path, and name tuples. It then removes those specified cookies by calling `CookieJar.clear()` for each matching tuple.",
      "The `RequestsCookieJar` class provides a dictionary-like interface for managing cookies while maintaining compatibility with Python's standard `http.cookiejar.CookieJar`. It is utilized in functions like `cookiejar_from_dict` and includes helper methods such as `get_dict` to easily convert cookies into key-value pairs.",
      "When given a name and value, requests constructs a `Cookie` object by calling the `create_cookie()` helper function with those parameters. This function is utilized by methods such as `RequestsCookieJar.set()` and `cookiejar_from_dict()` to convert key-value pairs into cookie instances.",
      "Requests builds the HTTP Basic Auth header value by passing the username and password into the `_basic_auth_str` helper function. This function is called within the `HTTPBasicAuth.__call__` method (or `proxy_headers`) to set the request's `Authorization` header.",
      "The `HTTPDigestAuth` class in `auth.py` implements HTTP Digest Authentication. It handles 401 challenge responses via its `handle_401` method and generates digest authentication headers with nonce tracking using `build_digest_header`.",
      "Requests resolves conflicts using the `merge_setting` function, which prioritizes the per-request setting over the session-level setting. If both settings are dictionaries, `merge_setting` merges them together so that request-level values override session-level values (and keys set to `None` are deleted).",
      "Requests uses special logic in the `merge_hooks` function because standard setting merging breaks session hooks entirely when request hooks are explicitly passed as `{'response': []}`. The function handles empty or `None` hook dictionaries to ensure session-level response hooks are properly preserved alongside request hooks.",
      "Requests strips the `Authorization` header in `rebuild_auth` if an `Authorization` header exists and `SessionRedirectMixin.should_strip_auth` returns `True`. This occurs whenever the original and redirected URLs have different hostnames, or when there is a change in scheme or port (excluding a standard http-to-https redirect on default ports).",
      "The `Session.send()` method follows redirect chains by consuming the `resolve_redirects` generator, persisting cookies across hops using `extract_cookies_to_jar`. During each redirect, `rebuild_proxies` re-evaluates proxy settings and proxy authorization headers, while `should_strip_auth` checks whether the `Authorization` header must be stripped due to changes in host, port, or scheme.",
      "When a redirect occurs, `SessionRedirectMixin.rebuild_proxies` re-evaluates proxy configuration by invoking `resolve_proxies` to account for environment variables like `NO_PROXY` against the new URL. It then updates or strips proxy settings accordingly and re-applies the `Proxy-Authorization` header for non-HTTPS schemes if proxy credentials are present.",
      "`requests` uses the `get_redirect_target` method in `SessionRedirectMixin` to extract the redirect URL from the response's `\"location\"` header. It checks if `Response.is_redirect` is true, re-encodes the `\"location\"` header in `latin1`, and decodes it as a UTF-8 native string.",
      "In `prepare_url`, the URL is stripped of leading whitespace, parsed, and checked for valid scheme and host requirements. For hosts containing non-ASCII characters, `prepare_url` uses `_get_idna_encoded_host` to encode the hostname with IDNA before reconstructing and requoting the final URL string.",
      "In `prepare_body`, requests determines the body format by checking the input arguments: if `data` is a non-standard iterable (like a generator), it is treated as a streamed body; otherwise, it prioritizes multipart file encoding if `files` is provided, form-encoded data if `data` is provided, or JSON encoding (`application/json`) if `json` is passed without `data`.",
      "A `Response` object determines if it represents an HTTP redirect through its `is_redirect` property. It checks if the `\"location\"` key is present in its `headers` dictionary and whether its `status_code` is contained within `REDIRECT_STATI`.",
      "When no encoding is specified on the `Response` object, the `Response.text` property falls back to using `Response.apparent_encoding`. This property detects the encoding from `self.content` using character detection libraries like `chardet` or `charset_normalizer`, defaulting to `\"utf-8\"` if no detection library is available.",
      "The `resolve_proxies` function checks if environment settings are trusted (`trust_env`) and that the URL should not be bypassed via `should_bypass_proxies`. It then retrieves environment proxy settings using `get_environ_proxies` and selects the proxy corresponding to the URL's scheme or falls back to the `\"all\"` key.",
      "Under the hood, `get()` sets `allow_redirects` to `True` by default and passes the parameters to `request()` with the HTTP method set to `\"GET\"`. The `request()` method then creates a `Request` object, converts it to a prepared request via `prepare_request()`, and sends it using `send()` to return a `Response` object."
    ]
  }
}