{
  "results": {
    "precision@k": 0.162962962962963,
    "recall@k": 0.7777777777777778
  },
  "predictions": [
    "Requests uses the `extract_cookies_to_jar` function to pull cookies from an HTTP response and store them in a `CookieJar`. It wraps the request and the response's original HTTP headers into `MockRequest` and `MockResponse` objects, then passes them to the cookie jar's `extract_cookies` method.",
    "Requests uses the `remove_cookie_by_name` function (which is wrapped by `RequestsCookieJar.__delitem__`) to iterate through the cookie jar and collect all matching `(domain, path, name)` tuples. It then loops through these clearable entries and calls `CookieJar.clear(domain, path, name)` to remove each matching cookie.",
    "The `RequestsCookieJar` class provides requests' cookie jar with a dict-like interface by implementing `MutableMapping`. It also inherits from `http.cookiejar.CookieJar`, maintaining full compatibility with Python's standard cookie jar operations.",
    "Requests uses the `create_cookie()` function to construct a `Cookie` object when provided with a name and value. This function is called by methods like `RequestsCookieJar.set()` and `cookiejar_from_dict()` before adding the created cookie to the jar using `set_cookie()`.",
    "Requests constructs the HTTP Basic Auth header value by calling the `_basic_auth_str` helper function with the provided username and password. This occurs within classes such as `HTTPBasicAuth` and `HTTPProxyAuth` (as well as the `proxy_headers` adapter function) to populate the `Authorization` or `Proxy-Authorization` headers.",
    "`HTTPDigestAuth` implements HTTP Digest Authentication in `requests`. It manages authentication state—including nonces and challenge information—per thread and handles 401 responses using its `handle_401` method to compute and send digest authorization headers.",
    "Per-request settings take precedence over session-level settings when a conflict occurs. The `Session` class handles this resolution by passing request-level settings first into helper functions like `merge_setting`, `merge_cookies`, and `merge_hooks` within methods such as `Session.prepare_request` and `Session.merge_environment_settings`.",
    "Requests uses the `merge_hooks` function because standard merging breaks `Session` hooks entirely when `request_hooks` contains an empty list like `{'response': []}`. The function checks for empty hook dictionaries to ensure session hooks are not improperly overwritten.",
    "In `SessionRedirectMixin.rebuild_auth`, the `Authorization` header is stripped whenever an `\"Authorization\"` header is present and `should_strip_auth` returns `True`. The `should_strip_auth` method returns `True` unconditionally if the hostnames of the original and redirected URLs differ (`old_parsed.hostname != new_parsed.hostname`), as well as when there are non-standard scheme or port changes.",
    "Requests handles HTTP redirect chains using `SessionRedirectMixin.resolve_redirects`, which loops through each redirect target until no location header remains or `max_redirects` is reached. For each hop, it updates cookies with `extract_cookies_to_jar` and `merge_cookies`, re-evaluates proxy configuration with `rebuild_proxies`, and updates or strips authentication using `rebuild_auth` and `should_strip_auth`.",
    "When a redirect occurs, `resolve_redirects` calls `rebuild_proxies`, which delegates to `resolve_proxies` to re-evaluate proxy settings against the new target URL (including checking `NO_PROXY` bypass rules and environment settings). `rebuild_proxies` then updates or removes the `Proxy-Authorization` header as needed for the redirected request.",
    "`requests` extracts the target URL using the `get_redirect_target` method, which checks if the `Response` is a redirect via `is_redirect`. If so, it retrieves the `\"location\"` header from `resp.headers` and re-encodes it into a native UTF-8 string using `to_native_string`.",
    "Requests validates and normalizes URLs in `prepare_url` by verifying the presence of a scheme and host, encoding query parameters, and requoting the final URL using `urlunparse` and `requote_uri`. For non-ASCII hostnames, `prepare_url` delegates to `_get_idna_encoded_host` to apply UTS-46 IDNA encoding, raising an `InvalidURL` exception if the hostname or IDNA encoding is invalid.",
    "In `prepare_body`, requests determines the body format by checking the types and presence of `json`, `data`, and `files`. It uses JSON if `json` is provided without `data`, treats non-standard iterables/generators in `data` as a streamed body, encodes using `_encode_files` if `files` are present, and otherwise defaults to form-encoded data via `_encode_params`.",
    "A `Response` object determines if it is an HTTP redirect using its `is_redirect` property. This property returns `True` if the `\"location\"` header is present in `headers` and the response's `status_code` exists in `REDIRECT_STATI`.",
    "When no encoding is explicitly set (`self.encoding` is `None`), the `Response.text` property falls back to guessing the encoding using `self.apparent_encoding` via `charset_normalizer` or `chardet`. Additionally, `get_encoding_from_headers` infers encoding from the HTTP `Content-Type` header, defaulting to `'ISO-8859-1'` for text content or `'utf-8'` for JSON.",
    "Requests determines proxy settings by first calling `should_bypass_proxies` to check if the URL matches any `no_proxy` environment variables, IP/CIDR rules, or system bypass settings. If the URL is not bypassed and environment settings are trusted, `resolve_proxies` uses `get_environ_proxies` to fetch proxies from environment variables and sets the proxy corresponding to the URL's scheme or the `\"all\"` fallback.",
    "Under the hood, the top-level `get()` function in `api.py` delegates execution by calling the top-level `request()` function with the `\"get\"` HTTP method. The `request()` function then creates a `sessions.Session()` context manager and calls `session.request()` to construct and send the HTTP request."
  ]
}